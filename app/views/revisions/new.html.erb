<h1>New revision</h1>

<%= render 'form' %>

<% if @question.there_is_at_least_one_revision %>
  <%= render 'table', revisions: @question.revisions %>
<% end %>

<%= link_to 'Back', pages_home_path %>
